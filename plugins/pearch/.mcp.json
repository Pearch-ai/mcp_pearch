{
  "pearch": {
    "command": "uv",
    "args": ["run", "--with", "fastmcp", "fastmcp", "run", "${CLAUDE_PLUGIN_ROOT}/../../pearch_mcp.py"],
    "env": {
      "PEARCH_API_KEY": "${PEARCH_API_KEY}"
    }
  }
}
